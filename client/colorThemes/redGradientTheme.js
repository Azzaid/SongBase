/**
 * Created by Johanas on 09.05.2018.
 */

import { createMuiTheme } from 'material-ui/styles';

const theme = createMuiTheme({
  palette: {
    type: "dark",
    background:{
      paper: 'rgba(170, 170, 170, 0.5);',
    }
  }});

export default theme;